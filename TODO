full text indexing (postgresql 8.3)

tags

web server (twisted, or separate process?)

detect updated entries & mark them unread

web: routes: spit out method not allowed

if an entry has no title, use the start of the content

relative URLs -- pass base to feadparser somehow?
manifests in the first post to 'revealing errors' and also post #2875

css leakthrough in entry #2828

better text->html conversion... note no line breaks in #3579
also linkify!

use clearing brs to fix problems caused by floated images

pass encoding to feedparser somehow (e.g., #4291)
